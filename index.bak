<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<title>Main Mancing Seru</title>

<style>
  body {
    font-family: Arial, sans-serif;
    background: #dff6f9;
    padding: 20px;
  }

  h1 { text-align: center; }

  .box {
    background: white;
    padding: 16px;
    border-radius: 12px;
    margin-bottom: 20px;
  }

  button {
    padding: 12px 16px;
    border-radius: 8px;
    border: none;
    background: #4fc3f7;
    font-weight: bold;
    cursor: pointer;
    margin: 10px 0;
    width: 100%;
  }

  button:disabled {
    background: #ccc;
    cursor: not-allowed;
  }

  .small {
    font-size: 14px;
    color: #555;
  }
</style>
</head>

<body>

<h1>🎣 Main Mancing Seru 🎣</h1>

<div class="box">
  <p>Uang: Rp <b id="uang">0</b></p>
  <p>Pancingan aktif: <b id="rodAktif">Pancingan Kayu</b></p>
  <button id="btnMancing">Mancing!</button>
  <p id="status" class="small"></p>
</div>

<div class="box">
  <h3>Info Ikan</h3>
  <div id="infoIkan"></div>
</div>

<div class="box">
  <h3>Toko Pancingan</h3>
  <div id="toko"></div>
</div>

<p class="small" style="text-align:center">
  Main Mancing, dibuat oleh Tama 2026
</p>

<script>
/* ================= STATE ================= */

let uang = 0;
let isMancing = false;

/* ================= PANCINGAN ================= */

const rods = {
  kayu: { nama: "Pancingan Kayu", luck: 0, owned: true },
  plastik: { nama: "Pancingan Plastik", harga: 150000, luck: 1, owned: false },
  besi: { nama: "Pancingan Besi", harga: 200000, luck: 2, owned: false },
  karbon: { nama: "Pancingan Karbon", harga: 500000, luck: 3, owned: false },
  graphite: { nama: "Pancingan Graphite", harga: 700000, luck: 4, owned: false }
};

let rodAktif = "kayu";

/* ================= IKAN (PAKE RANGE HARGA) ================= */

const fishes = [
  { nama: "Salmon", emoji: "🐟", tipe: "Common", minPrice: 5000, maxPrice: 8000, minLuck: 0, load: 1500 },
  { nama: "Ikan Hias", emoji: "🐠", tipe: "Uncommon", minPrice: 10000, maxPrice: 15000, minLuck: 0, load: 1800 },
  { nama: "Lumba-lumba", emoji: "🐬", tipe: "Epic", minPrice: 15000, maxPrice: 20000, minLuck: 1, load: 2300 },
  { nama: "Hiu", emoji: "🦈", tipe: "Legendary", minPrice: 25000, maxPrice: 25000, minLuck: 2, load: 3000 },
  { nama: "Paus Raksasa", emoji: "🐋", tipe: "Legendary+", minPrice: 30000, maxPrice: 40000, minLuck: 2, load: 3500 },
  { nama: "Kraken", emoji: "🐙", tipe: "Mythic", minPrice: 40000, maxPrice: 50000, minLuck: 3, load: 4200 },
  { nama: "Sotong Langka", emoji: "🦑", tipe: "Mythical Glory", minPrice: 90000, maxPrice: 90000, minLuck: 3, load: 4800 },
  { nama: "Kepiting Sunda", emoji: "🦀", tipe: "Mythical Super Glory", minPrice: 500000, maxPrice: 500000, minLuck: 4, load: 5500 }
];

/* ================= UTIL ================= */

function randomHarga(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}

/* ================= RENDER ================= */

function renderUang() {
  document.getElementById("uang").textContent =
    uang.toLocaleString("id-ID");
}

function renderRod() {
  document.getElementById("rodAktif").textContent =
    rods[rodAktif].nama;
}

function renderInfoIkan() {
  const div = document.getElementById("infoIkan");
  div.innerHTML = "";
  fishes.forEach(f => {
    div.innerHTML += `
      <p>${f.emoji} <b>${f.nama}</b><br>
      <span class="small">
        ${f.tipe} | Rp ${f.minPrice.toLocaleString("id-ID")} - ${f.maxPrice.toLocaleString("id-ID")}
      </span></p>
    `;
  });
}

function renderToko() {
  const div = document.getElementById("toko");
  div.innerHTML = "";
  Object.keys(rods).forEach(key => {
    const r = rods[key];
    if (r.owned) return;
    div.innerHTML += `
      <button onclick="beliRod('${key}')">
        Beli ${r.nama} - Rp ${r.harga.toLocaleString("id-ID")}
      </button>
    `;
  });
}

/* ================= LOGIC ================= */

function beliRod(key) {
  const r = rods[key];
  if (uang < r.harga) {
    alert("Uang kurang.");
    return;
  }
  uang -= r.harga;
  r.owned = true;
  rodAktif = key;
  renderUang();
  renderRod();
  renderToko();
}

document.getElementById("btnMancing").onclick = function () {
  if (isMancing) return;

  isMancing = true;
  this.disabled = true;
  document.getElementById("status").textContent =
    "🎣 Lagi narik pancing...";

  const luck = rods[rodAktif].luck;
  const pool = fishes.filter(f => f.minLuck <= luck);
  const fish = pool[Math.floor(Math.random() * pool.length)];
  const hargaDapat = randomHarga(fish.minPrice, fish.maxPrice);

  setTimeout(() => {
    uang += hargaDapat;
    document.getElementById("status").textContent =
      `DAPET ${fish.emoji} ${fish.nama} (${fish.tipe}) +Rp ${hargaDapat.toLocaleString("id-ID")}`;
    renderUang();
    isMancing = false;
    document.getElementById("btnMancing").disabled = false;
  }, fish.load);
};

/* ================= INIT ================= */

renderUang();
renderRod();
renderInfoIkan();
renderToko();
</script>

</body>
</html>